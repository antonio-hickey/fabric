[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
fabric-cache = { path = "../server" }
fabric-cache-client = { path = "../client" }
tokio = { version = "1", features = ["full"] }
serde_json = "1.0.133"
serde = { version = "1.0", features = ["derive"] }
